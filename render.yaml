# ProblemPulse — Render Deployment Config
services:
  - type: web
    name: problem-pulse
    env: node
    region: oregon
    buildCommand: cd backend && npm install && npm run build
    startCommand: cd backend && node index.js
    envVars:
      - key: PORT
        value: "10000"
      - key: NODE_ENV
        value: production